# ⚠️ 多说话人（MultiSpeakerTTS）功能未发布到 PyPI，必须从本仓库根目录安装：
# 下面的 -e .. 会从 API 目录指向仓库根，自动安装本地的 gsv_tts 包
-e ..

fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
requests>=2.28.0
httpx>=0.24.0
aiortc>=1.14.0
aiohttp>=3.14.1

# 可选：仅当使用 --use_asr 参数时需要（Qwen3-ASR 自动转录）
qwen-asr
